When someone clones the YT_Scoreboard repo:
git clone repo link
git submodule update --init --recursive

If the submodule is updated (the cv code):
#pull the latest commit
git submodule update --remote

